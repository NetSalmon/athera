[package]
name = "athera-rand"
version = "0.1.0"
edition = "2024"

[lib]
test = false
bench = false
doctest = false

[dependencies]
rand_chacha = { version = "0.10.0", default-features = false }
rand_core = { version = "0.10", default-features = false }
